<Window x:Class="TestTool.tc261.DataGridTest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestTool.tc261"     
        xmlns:cos="clr-namespace:Demo.Windows.Controls.filterDataGrid;assembly=Demo.Windows.Controls"      
        mc:Ignorable="d"
        Title="DataGridTest" Height="450" Width="800">
    <Grid>
        <!--<cos:FilterDataGrid
       x:Name="Fdg_AutoGenerateColumns"
       CanDragSum="True"
       CanUserGroup="True"
       FooterItems="{Binding FooterList}"
       HeadersVisibility="All"
       IsReadOnly="True"
       ItemsSource="{Binding DisplayEmployes, IsAsync=True}"
       ScrollViewer.CanContentScroll="True"
       SelectionUnit="Cell"
       ShowRowIndex="True"
       TotalFooterVisibility="Visible" />-->
      <ContentControl Content="{Binding FilterDataGridControl}" />
    </Grid>
</Window>
