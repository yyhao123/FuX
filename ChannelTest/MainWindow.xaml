<Window
    x:Class="ChannelTest.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Demo="https://shunnet.top"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ChannelTest"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Window.DataContext>
        <local:MainWindowViewModel />
    </Window.DataContext>
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <TreeView ItemsSource="{Binding Node}">

                <b:Interaction.Triggers>
                    <Demo:RoutedEventTrigger RoutedEventName="TreeViewItem.Expanded">
                        <Demo:EventCommand Command="{Binding TreeViewItem_Expanded}" />
                    </Demo:RoutedEventTrigger>
                </b:Interaction.Triggers>
                <ItemsControl.ItemTemplate>
                    <HierarchicalDataTemplate DataType="{x:Type local:OpcUaNodeBrowseStructuralBody}" ItemsSource="{Binding Children}">
                        <StackPanel Margin="0,5,0,5">
                            <Grid>
                                <Grid.ColumnDefinitions>

                                    <ColumnDefinition />
                                    <ColumnDefinition />
                                    <ColumnDefinition />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <RadioButton Grid.Column="0" />
                                <Image
                                    Grid.Column="1"
                                    Width="11"
                                    Height="11"
                                    Margin="0,0,5,0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Source="{Binding Icon}"
                                    Stretch="Fill" />
                                <TextBlock
                                    Grid.Column="2"
                                    Margin="0,0,5,0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="13"
                                    Text="{Binding Name}" />
                                <TextBlock
                                    Grid.Column="3"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="11"
                                    Text="{Binding Count}" />
                            </Grid>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </ItemsControl.ItemTemplate>


            </TreeView>
        </Grid>
        <Grid Grid.Row="1">
            <Button Content="测试">
                <b:Interaction.Triggers>
                    <b:EventTrigger EventName="Click">
                        <Demo:EventCommand Command="{Binding ClickCommand}" />
                    </b:EventTrigger>
                </b:Interaction.Triggers>
            </Button>

        </Grid>



    </Grid>
</Window>
