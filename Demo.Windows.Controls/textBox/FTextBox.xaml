<UserControl
    x:Class="Demo.Windows.Controls.textBox.FTextBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Demo.Windows.Controls.textBox"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://materialdesigninxaml.net/winfx/xaml/themes"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <!--  输入框左侧标题  -->
        <TextBlock
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType=UserControl}}"
            FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
            FontStretch="{Binding FontStretch, RelativeSource={RelativeSource AncestorType=UserControl}}"
            FontStyle="{Binding FontStyle, RelativeSource={RelativeSource AncestorType=UserControl}}"
            FontWeight="{Binding FontWeight, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
            TextAlignment="Center" />

        <Grid Grid.Column="1">
            <!--  文本输入框  -->
            <local:XEdit
                Width="{Binding Width, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Height="{Binding Height, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Padding="0,0,0,1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                BorderThickness="1"
                FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType=UserControl}}"
                FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                FontStretch="{Binding FontStretch, RelativeSource={RelativeSource AncestorType=UserControl}}"
                FontStyle="{Binding FontStyle, RelativeSource={RelativeSource AncestorType=UserControl}}"
                FontWeight="{Binding FontWeight, RelativeSource={RelativeSource AncestorType=UserControl}}"
                IsAutoSize="False"
                Text="{Binding Content, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource AncestorType=UserControl}}"
                TextAlignment="Left" />
        </Grid>

    </Grid>
</UserControl>
