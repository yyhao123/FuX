<UserControl
    x:Class="Demo.AutoTest.view.userControls.MiddleGraphArea"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Demo.AutoTest.view.userControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="800"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="1" />
            <RowDefinition Height="1*                                                        " />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="1" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <!--  伪彩色界面  -->
        <Grid>
            <ContentControl x:Name="TopLeft" Content="{Binding TopLeftView}" />
        </Grid>
        <!--  光谱采集界面  -->
        <Grid Grid.Column="3">
            <ContentControl x:Name="TopRight" Content="{Binding TopRightView}" />
        </Grid>
        <!--  操作界面  -->
        <Grid Grid.Row="3" Grid.Column="3">
            <ContentControl x:Name="RightBottom" Content="{Binding RightBottomView}" />
        </Grid>
    </Grid>
</UserControl>
