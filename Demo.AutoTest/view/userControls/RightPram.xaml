<UserControl
    x:Class="Demo.AutoTest.view.userControls.RightPram"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:FuX="https://FuX.top"
    xmlns:local="clr-namespace:Demo.AutoTest.view.userControls"
    xmlns:m="clr-namespace:Demo.AutoTest.Model.data;assembly=Demo.AutoTest.Model"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pt="http://propertytools.org/wpf"
    d:DesignHeight="800"
    d:DesignWidth="300"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <FuX:PropertyControl
            BasicsData="{Binding SpectrumEt}"
            ControlFactory="{Binding CustomPropertyGridControlFactory}"
            Operator="{Binding CustomPropertyGridOperator}" />

        <FuX:ButtonControl
            Grid.Row="1"
            Command="{Binding SetParmCommand}"
            Content="设置" />
    </Grid>
</UserControl>
